Options -Indexes

<IfModule mod_rewrite.c>
RewriteEngine On
RewriteBase /

# tillat ekte filer/mapper
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# alt annet -> Angular
RewriteRule . /index.html [L]
</IfModule>
